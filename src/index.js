import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); //const to pass "reactdom.createroot" at a certain element - "root"
root.render( //render the content passed inside of the app component
//UI Design here
    <App />
);

